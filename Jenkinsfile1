pipeline {
  agent any
   stages {
      stage('build') {
          steps {
                sh 'docker build -t DockerFile1:2'
                
                }
      }    
        stage('Test') {
              steps {
                     sh 'docker build -t DockerFile1:2'
                     sh 'docker run  DockerFile1:2'
                    echo "docker image tested"
                    }
           }
          stage('Deploy') {
              steps {
              sh 'docker run  DockerFile1:2'
                    sh 'sudo docker push naveenkumaraluthuri DockerFile1'
                  }
            }
  }
 }
