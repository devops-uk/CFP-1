pipeline {
  agent any
   stages {
      stage('Test') {
              steps {
                 
                     sh 'docker run -it -d Dockerfile'
                    echo "docker image tested"
                    }
           }
          stage('Deploy') {
              steps {
              sh 'docker run -it -d Dockerfile'
                    sh 'sudo docker push naveenkumaraluthuri Dockerfile'
                  }
            }
  }
 }
